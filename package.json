{
  "name": "multiplayer-trading-game",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build && esbuild server/index.ts --outfile=dist/index.js --platform=node --packages=external --bundle --format=esm",
    "start": "NODE_ENV=production node dist/index.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "ws": "^8.13.0"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "typescript": "^5.4.0",
    "vite": "^5.0.0"
  },
  "type": "module"
}
